import { HostHome } from '@/components/host/host-home';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Host' };

export default function HostPage() {
  return <HostHome />;
}

import { AccountProfilePage } from '@/components/account/profile';

export const dynamic = 'force-dynamic';
export const metadata = { title: 'Profile' };

export default function ProfilePage() {
  return <AccountProfilePage />;
}
